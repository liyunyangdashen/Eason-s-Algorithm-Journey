#include<iostream>
using namespace std;
int n,x,ans;
int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    cin >> n;
    while (n--){
        cin >> x;
        ans ^= x;
    }
    cout << ans;
    return 0;
}
