#include<iostream>
using namespace std;
int n,k,last; long long cnt;
int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    cin >> n >> k;
    while (n){
        cnt += n;
        n += last;
        last = n % k;
        n /= k;
    }
    cout << cnt;
    return 0;
}
