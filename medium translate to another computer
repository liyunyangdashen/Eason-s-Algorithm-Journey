#include<iostream>
#include<vector>
#include<utility>
#include<cmath>
using namespace std; const int N = 2e5 + 5; 
using pii = pair<int,int>; using piii = pair<int,pii>;
int n,rt,u,v,sw,mw,dep[N],fa[N][20],t[N],s[N]; piii e[N]; vector<piii> adj[N]; bool ind[N];
void dfs(int st,int f){
    fa[st][0] = f;
    dep[st] = dep[f] + 1;
    for (auto e : adj[st]){
        int v = e.first;
        if (v != f)
            dfs(v,st);
    }
}
int LCA(int x,int y){
    
}
int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    cin >> n;
    for (int i = 1;i < n;i++){
        cin >> u >> v >> sw >> mw;
        adj[u].push_back(make_pair(v,make_pair(sw,mw)));
        adj[v].push_back(make_pair(u,make_pair(sw,mw)));
        e[v] = make_pair(u,make_pair(sw,mw));
        if (!ind[u]) rt = u;
        ind[v] = 1;
    }
    dfs(rt,0);
    for (int j = 1;j <= log2(n);j++)
        for (int i = 1;i <= n;i++)
            fa[i][j] = fa[fa[i][j - 1]][j - 1];
    for (int i = 1;i < n;i++){
        int u = i,v = i + 1;
        int lca = LCA(u,v);
        t[u]++, t[v]++, t[lca] -= 2;
    }
    dfs_t(rt,0);
    for (int i = 2;i <= n;i++){
        
    }
    return 0;
}
