#include<iostream>
using namespace std;
unsigned int n;
int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    cin >> n;
    cout << (n << 16) + (n >> 16);
    return 0;
}
